# mcp 2.0.0 removed mcp.server.fastmcp, which index.py imports. Vercel's build
# predates that release; an unpinned install on a fresh host resolves to 2.x and
# dies at import. Lift the ceiling only when the server is ported.
mcp>=1.2.0,<2
fastapi>=0.110
uvicorn[standard]>=0.30
psycopg[binary]>=3.1
psycopg-pool>=3.2
python-dotenv>=1.0.0
requests>=2.31
numpy>=2.0
